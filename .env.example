# Created automatically when you link Upstash Redis to the project:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: require this token for writes (set the same value in Vercel + below for the client build)
SYNC_WRITE_KEY=
VITE_SYNC_WRITE_KEY=
